{
  "buildCommand": "pnpm install && npx expo export -p web",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "EXPO_USE_METRO_WORKSPACE_ROOT": "1"
  }
}
